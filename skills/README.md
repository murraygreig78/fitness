# JSON Activity Tracker skills

The downloadable pack lives in [`static/skills/json-activity-tracker/`](../static/skills/json-activity-tracker/) so the app can serve it from Admin.

Author the markdown there (or copy from this folder), then keep both copies in sync. Admin → **Download AI skills** zips those files plus a combined `PROMPT.md`.
